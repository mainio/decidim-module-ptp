<%= decidim_modal id: "budget-confirm", class:"verification-modal" do %>
  <% if current_order.present? %>
    <div data-dialog-container>
      <%= icon "check-double-line" %>
      <h2 class="h2" id="dialog-title-budget-confirm" data-dialog-title>
        <%= t("title", scope: "decidim.budgets.projects.budget_confirm") %>
      </h2>
      <div class="my-8" id="budget-confirm-current-order">
        <% if current_order.present? %>
          <h4 class="h4 mb-4"><%= translated_attribute(budget.title) %></h4>
          <p class="mb-4 text-md"><%= t(".description") %></p>
          <ul class="budget-confirm__selected-list divide-y border w-full">
            <% current_order.projects.each do |project| %>
              <li class="budget-confirm__selected-item space-y-2 pl-2 py-2 flex flex-col md:py-0 md:space-y-0 md:pl-0 md:flex-row">
                <div class="w-4/6 flex items-center justify-start font-bold text-lg md:text-center md:text-xl md:pl-6 md:border-r">
                  <%= translated_attribute(project.title) %>
                </div>
                <div class="w-2/6 flex items-center md:justify-center md:text-center">
                  <strong class="budget-confirm__selected-number">
                    <%= budget_to_currency project.budget_amount %>
                  </strong>
                </div>
              </li>
            <% end %>
          </ul>
        <% end %>
      </div>
    </div>

    <div data-dialog-actions>

      <button class="button button__lg button__transparent-secondary text-md rounded-none sm:text-lg" data-dialog-close="budget-confirm">
        <%= t("cancel", scope: "decidim.budgets.projects.budget_confirm") %>
      </button>

      <%= button_to checkout_budget_order_path(budget), class: "button button__lg button__secondary rounded-none text-md sm:text-lg", data: { disable: true } do %>
        <span>
          <%= t("confirm", scope: "decidim.budgets.projects.budget_confirm") %>
        </span>
        <%= icon "arrow-right-line", class: "fill-current" %>
      <% end %>
    </div>
  <% end %>
<% end %>
